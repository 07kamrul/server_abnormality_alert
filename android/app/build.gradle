plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.colgis.server_abnormality_alert"
    compileSdk = 35 // Ensure this matches your project requirements
    lintOptions {
        disable 'Deprecation'
    }
    defaultConfig {
        applicationId = "com.colgis.server_abnormality_alert"
        minSdk = 21 // Adjust as needed based on your dependencies
        targetSdk = 33
        versionCode = 1 // Update this as per your versioning
        versionName = "1.0" // Update as per your release
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug // Use release signingConfig for production
            minifyEnabled false
            shrinkResources false
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
        }
    }
}

flutter {
    source = "../.."
}
